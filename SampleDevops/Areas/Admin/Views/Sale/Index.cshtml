<!--OpenCatapultModelId:6-->
@model List<SampleDevops.Areas.Admin.Models.SaleViewModel>
@{
    ViewData["Title"] = "View Sale";
}
<h2>@ViewData["Title"]</h2>
<div>
    <a asp-area="Admin" asp-controller="Sale" asp-action="Create" class="btn btn-default">Create</a>
</div>
<div>&nbsp;</div>
<table class="table">
    <tr>
        <th>Description</th>
        <th>Title</th>
        <th>Start Date</th>
        <th>End Date</th>
        <th></th>
    </tr>
@foreach (var item in Model)
{
    <tr>
        <td>@item.Description</td>
        <td>@item.Title</td>
        <td>@item.StartDate</td>
        <td>@item.EndDate</td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </td>
    </tr>
}
</table>
