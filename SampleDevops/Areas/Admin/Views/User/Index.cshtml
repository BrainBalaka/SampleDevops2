<!--OpenCatapultModelId:1-->
@model List<SampleDevops.Areas.Admin.Models.UserViewModel>
@{
    ViewData["Title"] = "View User";
}
<h2>@ViewData["Title"]</h2>
<div>
    <a asp-area="Admin" asp-controller="User" asp-action="Create" class="btn btn-default">Create</a>
</div>
<div>&nbsp;</div>
<table class="table">
    <tr>
        <th>User name</th>
        <th>Email</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Is Active?</th>
        <th></th>
    </tr>
@foreach (var item in Model)
{
    <tr>
        <td>@item.UserName</td>
        <td>@item.Email</td>
        <td>@item.FirstName</td>
        <td>@item.LastName</td>
        <td>@item.IsActive</td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </td>
    </tr>
}
</table>
